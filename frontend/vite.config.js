import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Standard Vite config for a React project — no changes needed here.
export default defineConfig({
  plugins: [react()],
});
